name: Code Quality Check

on: [push, pull_request]

jobs:
  code-quality:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'
        extensions: mbstring, dom, xml
        tools: composer

    - name: Install dependencies
      run: |
        composer require --dev squizlabs/php_codesniffer
        composer require --dev friendsofphp/php-cs-fixer

    - name: Auto-fix coding standards with PHPCBF
      run: |
        echo "🔧 Автоматическое исправление стандартов кода..."
        ./vendor/bin/phpcbf --standard=PSR12 ./class --report=summary || true

    - name: Run PHP Code Sniffer (после исправлений)
      run: |
        echo "📊 Проверка стандартов кода после исправлений..."
        ./vendor/bin/phpcs --standard=PSR12 ./class --report=summary --warning-severity=0 || echo "Есть ошибки, но продолжаем..."

    - name: Run PHP-CS-Fixer для дополнительного исправления
      run: |
        echo "🎨 Дополнительное форматирование..."
        ./vendor/bin/php-cs-fixer fix ./class --rules=@PSR12 --allow-risky=yes --diff || true

    - name: Check for syntax errors
  run: |
    echo "🔍 Проверка синтаксических ошибок..."
    find ./class -name "*.php" -exec php -l {} \; | grep -v "No syntax errors" || true

    - name: Commit auto-fixed code (если есть изменения)
      run: |
        if [ -n "$(git status --porcelain)" ]; then
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add ./class/
          git commit -m "🚀 Auto-fix coding standards to PSR-12"
          git push
          echo "✅ Автоисправления закоммичены и отправлены"
        else
          echo "✅ Изменений для коммита нет"
        fi
